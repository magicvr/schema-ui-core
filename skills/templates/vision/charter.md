---
doc_type: vision-charter
vision_id: vision-example-project
title: <项目愿景标题>
status: active
version: 0.1.0
effective_date: YYYY-MM-DD
primary_workspace: 
created: YYYY-MM-DD
updated: YYYY-MM-DD
---

# Charter · <项目名>

> 复制为 `docs/vision/charter.md` 后改写。每项目**有且仅有一个**现行 active Charter（单愿景制）。  
> 最小完备见 P-006 / alignment。不可使用 Goal 的 `done`。

## 目的陈述

<一句话到一短段：长期要成为什么>

## 方向级成功边界

在本愿景仍为 `active` 的前提下，下列方向成立即视为仍在愿景内（**不是**可关门验收 checklist）：

1. <边界 1>
2. <边界 2>
3. <边界 3>

## 非目标

本 Charter **不**要求、也**不**把下列事项写成愿景成功条件：

- <非目标 1>
- <非目标 2>
- <非目标 3>

## 原则摘要

操作原则以 `docs/architecture/principles.md` 为准（P-001～P-006）。与本 Charter 直接相关的要点可在此摘要。

## 战略假设（可选）

| id | 假设 / 未知 | 影响 | 状态 |
|----|-------------|------|------|
| H-001 | <…> | 方向已稳 / 某 VP | open |

## 与工作区 / VP 的关系

- 本 Charter 是对齐链**源头**；不可 Goal-`done`。
- 意图落盘为 `docs/vision/plans/VP-*.md`，经 `vision_ref` 对齐本文件版本。
- 工作区/Root 挂 `plan_refs` / `primary_plan`；不在此维护 progress%。

## 现行版本

| 项 | 值 |
|----|-----|
| `vision_id` | `vision-example-project` |
| 版本 | `0.1.0` |
| 状态 | `active` |

修订史见 `docs/vision/revisions.md`。引用格式：`vision-example-project@0.1.0`。
