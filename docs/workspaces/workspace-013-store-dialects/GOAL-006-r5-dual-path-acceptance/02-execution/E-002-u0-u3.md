---
id: E-002
doc: execution-entry
goal: GOAL-006-r5-dual-path-acceptance
status: recorded
created: 2026-08-20
updated: 2026-08-20
version: 1.0.0
---

# E-002 · U0–U3 实施（升级策略 + 备份合同 + 跨模块共事务）

## 2026-08-20 · R5 主体落地

### 已发生事实

- **U0 基线**：sqlite `go test ./...` **0 FAIL**；live PG `TestFullCatalogPostgresBootstrapIntegration`（48 迁移 boot + 台账 + BIGINT 合规）+ `TestCompositionPostgresStartup`（完整启动 + readyz 门禁）全绿。
- **U1 · 升级策略（I-001 → verified）**：D-002 结论——in-place 跨引擎不可行（书面 residual）；支持 **fresh bootstrap（dual catalog apply，live 已验证）** + **逻辑数据迁移（dump/restore / 模块导入）**。
- **U2 · 备份/恢复合同（I-004 → verified）**：D-002 合同 — 逻辑备份 `pg_dump -F c` → `pg_restore`；物理/PITR `pg_basebackup` 任选；恢复验证=「dump→restore→数据/台账核对」。**可执行验证**：live PG round-trip——r5u2（建 users 表 + 2 行）→ `pg_dump -F c` → 新建 r5u2rest → `pg_restore` → `SELECT count(*) = 2` ✅。
- **U3 · 跨模块共事务**：新增 `TestPostgresCrossModuleSharedTx`（live PG）——authsession `CreateServiceCredential` + operationlog `RecordOperationTx` 同事务：提交路径 cred+op 各 1 行；审计故障路径双双回滚（0/0）。**PASS**。

### 证据

| 主张 | 路径 / 命令 / commit |
|------|----------------------|
| 决策 D-002 | `GOAL-006-r5-dual-path-acceptance/01-decision/D-002-upgrade-backup-contract.md` |
| 备份 round-trip | `docker exec r2-pg-probe pg_dump -F c … ; pg_restore …`（r5u2rest count=2） |
| 跨模块共事务 | `apps/api/internal/store/postgres_test.go` `TestPostgresCrossModuleSharedTx`（live PG PASS） |
| 全量回归 | `apps/api`: `go test ./...` 0 FAIL |
