---
id: D-003
doc: decision-entry
goal: GOAL-001-account-email-identity
status: accepted
created: 2026-08-24
updated: 2026-08-24
version: 1.0.0
---

# D-003 · 解冻本 Root（VP-017 已按现行分母再次关门）

## 背景

D-002 设定的解冻条件（须同时）现已满足：

1. **VP-017 按现行渠道分母再次 `closed`**：v0.5.0（2026-08-24）。R5～R8 = GOAL-006～009 全部 done；Resend live 投递实跑 PASS；VRev-042（self）`pass`；开放 required = 0。
2. **用户明确解冻**：用户于 2026-08-24 审视会话中确认执行解冻同步。

愿景层已先行记录解冻：`VP-017` 门闩表「关门门闩（已解除）」、`VP-018` 状态表「active · 已解冻」、组合索引 `workspaces.md` 「active · 解冻」。本决策把实现层（工作区 018）对齐到该事实。

## 决定

1. Root `status: blocked → active`。`progress` 保持 0/4。
2. 纲领路线图 R1～R4 状态「冻结 → 待启动」；允许创建 R1 子目标与开展 I-001 / I-002 信息澄清。
3. 运输面验收对照 VP-017 现行交付的渠道模型（默认渠道以当时配置为准，预期 mock 出站记录），不以历史 CaptureSink `Last()` 或 SMTP 专用叙事为验收权威。
4. **R1 合同冻结门禁不变**：I-001（唯一性细则）/ I-002（验证码 vs 链接）须先 verified 或经用户接受残余，方可冻结 R1 身份合同；I-003 / I-004 维持 registered 投影。
5. `users` DDL 变更仍归 R2，且以 I-001 关闭为前置；本次解冻不改应用代码。

## 为什么

- 解冻条件客观成立且有落盘证据（VP-017 v0.5.0 关门事务 + VRev-042），继续维持 `blocked` 与事实不符。
- 用户裁决确认（D-002 条件 2 的「明确解冻」路径），非静默自动放行。

## 未选方案

- 维持 `blocked`：冻结前提已消失，等于无视愿景层已记录的解冻事实。
- 解冻同时直接冻结 R1 合同：I-001 / I-002 尚在 collecting，违反 P-005 阶段门禁。
- 取消或重排 R1～R4：纲领路线图（P-001）仍然有效，无需重写。

## 后续

- 可选下一步：创建 R1 子目标，推进 I-001 / I-002 澄清（两项均需用户裁决形态选择）。
- R1 合同冻结 → R2 schema（依赖 I-001）→ R3 消费 `MailSender`（I-005 前置）→ R4 证据。
