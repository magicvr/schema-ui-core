---
id: E-017-v0.7.0-postmerge-release-gates
parent_goal: GOAL-001-nav-group-collapsible
doc: execution-entry
status: recorded
date: 2026-09-21
created: 2026-09-21
updated: 2026-09-21
version: 0.1.0
---

# E-017 · v0.7.0 合并后发布门禁与 tag 前资产准备

## 已发生事实

1. 修复 PR [#17](https://github.com/magicvr/schema-ui-core/pull/17) 已于 2026-09-21 08:48:25Z 合并，merge commit `455df884754f7a16edd377644b60d75235e8e9bc`。PR head 为 `4af53755facb109139323e96d4cba8508cf739c2`。
2. PR CI run [35579164330](https://github.com/magicvr/schema-ui-core/actions/runs/35579164330) 的 9/9 jobs 全部 `success`；web job 内新增 package metadata/ESM tests、六包构建及真实 tarball 隔离消费者 smoke 通过。
3. 合并后 main CI run [35579828396](https://github.com/magicvr/schema-ui-core/actions/runs/35579828396) 对 merge SHA `455df884…` 的 9/9 jobs 全部 `success`。
4. 在 main merge commit 上重建、finalize 与 pack 后，实际发布并从 `registry.npmjs.org` 核实四个修复版：`@magicvr/schema-ui-protocol@0.2.17`、`@magicvr/schema-ui-lib@0.1.16`、`@magicvr/schema-ui-renderer@0.3.15`、`@magicvr/schema-ui-ui@0.1.13`。已有 shell `0.1.6` 与 theme `0.1.4` 按发布脚本幂等跳过。
5. OS 临时目录中的 registry 消费者执行 `npm install`，安装六个 registry 包、React peers 与声明依赖（20 packages）；六个包根入口与 protocol app-manifest / host-support / runtime-schema-validator / node schema JSON、lib utils、UI lib utils / button 共 13 项导入全部 `PASS`。四个修复版本的 `npm view <name>@<version> version` 均返回精确版本。
6. 对 main merge SHA 以 Go 1.26、`CGO_ENABLED=0` 交叉编译 linux/darwin/windows × amd64/arm64 六个 CLI 二进制；按现行 Release 命名制成六个 ZIP 与 `SHA256SUMS`。每个 ZIP 仅含对应 CLI 二进制；六个 SHA-256 本地复算匹配；`go version -m` 显示六个二进制的 `vcs.revision` 均为 `455df884…`。
7. 截至本条写入时，annotated tag `apps/api/v0.7.0` 尚未创建，六个 ZIP 与 SHA256SUMS 尚未上传，GitHub Release 尚未建立。资产当前位于本地临时目录，不是已发布资产。

## 证据边界

- 本地 registry 安装 / 导入 smoke 不替代 Hosted CI；PR 与 main CI 均有独立 run 证据并已全绿。
- npm 发布是外部写入事实，tag / Release 尚未发生；不得将本记录或本地资产当成 release 已完成。
- 本条只记录发布历史，不改变 Root / workspace / VP 状态。
