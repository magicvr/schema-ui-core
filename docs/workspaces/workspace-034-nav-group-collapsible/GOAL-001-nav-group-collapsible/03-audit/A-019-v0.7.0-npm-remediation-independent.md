---
id: A-019-v0.7.0-npm-remediation-independent
goal_id: GOAL-001-nav-group-collapsible
doc: audit-entry
source: independent
auditor: Grok Build grok-4.6 (high)
type: execution-facts
scope: uncommitted v0.7.0 npm ESM consumer remediation on codex/npm-esm-consumer-fix; A-018/E-016, current diff, workflow, finalization, package metadata, hardened isolated tarball smoke
date: 2026-09-21
verdict: pass
created: 2026-09-21
updated: 2026-09-21
parent: null
version: 0.1.0
---

# A-019 · v0.7.0 npm 消费修复 independent 审计（2026-09-21）

- **source**：independent
- **auditor**：Grok Build grok-4.6 (high)
- **类型** / **scope**：execution-facts · `codex/npm-esm-consumer-fix` 上未提交的 v0.7.0 npm ESM 消费修复；核对 A-018 / E-016、工作树 diff、CI workflow、finalization、包元数据，以及加固后的真实 tarball 隔离 smoke。对照 A-018 F-S-002 `fixed`。
- **verdict**：**pass**（开 PR 前无未关闭 high required；Hosted PR CI 与修复版 npm/tag/Release 仍是后续独立门禁，不记为已完成。）

## 范围与区间

工作区：`workspace-034-nav-group-collapsible`（`root_goal: GOAL-001-nav-group-collapsible`，`canonical_scope: docs/workspaces/workspace-034-nav-group-collapsible/`，`shared_materials_catalog: none`，`primary_plan: VP-034-nav-group-collapsible`）。Charter `schema-ui-core-admin-foundation@0.4.0` `active`。本意见只审本区 Root 的发布包修复候选，未读取其他工作区目标正文。

审计对象：

- 分支 `codex/npm-esm-consumer-fix`，HEAD `d083e337`（PR #16 merge）加上未提交工作树
- [A-018](A-018-v0.7.0-npm-remediation-self.md) self 与 [E-016](../02-execution/E-016-npm-consumer-remediation.md)
- 现行脚本：`scripts/package-esm-compat.mjs`、`scripts/package-esm-compat.test.mjs`、`scripts/rewrite-lib-aliases.mjs`、`scripts/finalize-lib-dist.mjs`、`scripts/npm-package-consumer-smoke.mjs`
- `.github/workflows/r6-basic-matrix.yml` web job、CLI/QUICKSTART 版本钉、六包 `versions` / peers
- 本会话对现有 `apps/web/dist-lib/` 产物与现行 smoke 的独立复跑

按用户书面范围：**未**重复查询已发布损坏版本在 registry 上的内容；E-016 记录的 `@magicvr/schema-ui-protocol@0.2.16` / lib `0.1.15` / renderer `0.3.14` / ui `0.1.12` 故障面视为已独立确认。Hosted PR CI 尚未运行，本条不把它写成通过。

本意见未修改 `status` / `progress` / goal-tree / 决策或执行正文 / 实现代码。

## 成果（有证据）

| 检查项 | 结果 | 证据 |
|---|---|---|
| 工作区绑定 | 合格 | `workspace.md` Root/canonical/`plan_refs`+`primary_plan` 一致；资料目录 `none` |
| P-005 信息项 | 不影响本门禁 | I-034-001～005 均 `verified`，最晚阶段为 R1–R5；本修复未引入新的到期 required 信息项 |
| 工作树范围 | 可核对 | 已跟踪改动：workflow、QUICKSTART、CLI 版本钉、`rewrite-lib-aliases.mjs`、E-016/A-018 索引。未跟踪：`scripts/package-esm-compat.mjs`、其测试、`scripts/npm-package-consumer-smoke.mjs`、E-016、A-018 |
| A-018 F-S-002 闭合 | 独立同意 `fixed` | rewrite 末尾 `await import("./finalize-lib-dist.mjs")`；CI web job 在 `npm ci` 后执行 build + rewrite + smoke；本会话 `npm pack --dry-run` 显示 protocol `0.2.17` tarball 含 `schemas/{action,node,page,reaction}.schema.json` 与 `protocol/host-support.json` |
| ESM 规范化 | 可重复核对 | `normalizePackageEsmImports` 把 `@schema-ui/*` / `@schemas/*.schema.json` 转为公开名、为相对路径与包子路径补 `.js`、为 JSON 补 `with { type: "json" }`。本会话 `node --test scripts/npmjs-package-name.test.mjs scripts/package-esm-compat.test.mjs` **8/8 pass** |
| 产物面 | 可核对 | `apps/web/dist-lib` 中无残留 `@schema-ui/` 或 `@schemas/`；无 `import(` 动态导入。renderer 根入口已使用公开 `.js` 子路径（ui/lib/protocol）。`runtime-schema-validate.js` 以 JSON attributes 导入四份 schema |
| 版本钉 | 一致 | CLI `apps/api/cmd/schema-ui/main.go`、QUICKSTART、`rewrite-lib-aliases.mjs` `versions` 均为 protocol `0.2.17` / lib `0.1.16` / renderer `0.3.15` / ui `0.1.13`；shell `0.1.6`、theme `0.1.4` 保持已发布版本。peers：protocol→lib `^0.1.16`，ui→lib `^0.1.16`，renderer 钉到上述新 patch；shell peer 仍为 protocol `^0.2.12`（覆盖 `0.2.17`） |
| 加固后隔离 smoke | 本会话复跑通过 | 现行 `scripts/npm-package-consumer-smoke.mjs`：在 `os.tmpdir()` 下 `mkdtemp`，对六包 `npm pack`，再 `npm install` 六个本地 tarball + `react@19` + `react-dom@19`（本会话 **added 20 packages**），然后动态导入 6 个根入口与 7 个代表子路径。本会话 **13 PASS**，exit 0。cleanup 仅删除 `os.tmpdir()` 下 `schema-ui-npm-consumer-*` |
| CI 配置 | 已写入候选，Hosted 未跑 | `r6-basic-matrix.yml` web job：仓库根跑 mapping+ESM 单测；`apps/web` `npm ci` 后于仓库根跑 build / rewrite / smoke。Linux Node 22。Hosted PR CI **尚未运行** |
| 外部发布状态 | 未发生（修复版） | 四个修复 patch、`apps/api/v0.7.0` tag、GitHub Release 均未创建。E-016 亦如此记录 |

## 对照成功标准

| 标准 | 状态 | 证据 |
|------|------|------|
| 已发现的 Node ESM / schema 资产故障在修复候选中可消费 | 达到（本地 tarball） | 隔离 smoke 13/13；protocol tarball 含 schemas 与 host-support JSON |
| finalization 进入合并门禁 | 达到（配置） | rewrite 自动 finalize；CI 步骤调用 rewrite 后再 smoke |
| 版本钉与 create 包面一致 | 达到 | CLI / QUICKSTART / rewrite `versions`+peers 一致 |
| Hosted PR CI 全绿 | 未发生 | 尚无 PR run |
| 修复版已发布且 registry 可消费 | 未发生 | 后续门禁 |
| 不把 dry-run / 本地 smoke 写成 npm 发布完成 | 遵守 | E-016 / A-018 / 本条均未宣称上传 |

## Findings

### F-001 · Hosted PR CI 尚未运行，合并前须等新 web job 全绿

- 严重度：med
- 建议：recommended
- 状态：open
- 描述：现行 workflow 已把六包构建、finalize 与隔离 tarball smoke 接到 `r6-basic-matrix` 的 web job。这是 A-018 F-S-002 的回归门禁。Hosted PR CI 尚未产生 run，不能用本地 smoke 代替合并证据。开 PR 本身不被本条阻断。
- 证据：`.github/workflows/r6-basic-matrix.yml` 新增步骤；用户书面确认 Hosted PR CI 未跑；本会话无 GitHub Actions 成功记录。

### F-002 · 修复版 npm 仍须先于 `apps/api/v0.7.0` tag 可见

- 严重度：med
- 建议：recommended
- 状态：open（继承并更新 A-016 F-001）
- 描述：A-016 F-001 的顺序仍然有效，对象改为修复 patch：protocol `0.2.17`、lib `0.1.16`、renderer `0.3.15`、ui `0.1.13`。已发布的 `0.2.16` / `0.1.15` / `0.3.14` / `0.1.12` 组合按 E-016 不能作为 tag 时刻的消费面。合并且 main CI 全绿后，先发布并做 registry 隔离消费校验，再 annotated tag 与 GitHub Release。shell@0.1.6 与 theme@0.1.4 继续幂等跳过。
- 证据：CLI/QUICKSTART 已钉到修复版；E-016 记录修复包未发布、tag/Release 未创建。

无其他 required / recommended finding。下列缺口记录为范围事实，不升为 finding：

- 本会话未重跑 `go test ./cmd/schema-ui`；版本常量已在源码核对。
- 发布路径仍走 `pack-npm-packages.mjs` 的 `pnpm pack`，CI smoke 走 `npm pack`。本会话对 protocol 的 `npm pack --dry-run` 已看到 schemas 进入 tarball；二者 `files` 字段相同，不构成开 PR 阻断。
- smoke 安装的是本地重建的 shell@0.1.6 / theme@0.1.4，不是 registry 上已发布的同版本 tarball。E-016 记录旧六包组合导入曾通过，本次故障面在四个新 patch；不把该差异写成 required。
- E-016 / A-018 正文写「解包」；现行脚本是 `npm pack` 后对 tarball 做 `npm install`。实现强于该措辞，不构成产品缺陷。

## 必改项汇总

无。开 PR 前开放 required = 0。

## 与既有意见的异同

- 同意 A-018 对 F-S-002 的 `fixed`：finalize 已挂入 rewrite，真实 tarball 消费已进入脚本与 CI 配置；本会话对加固后 smoke（OS temp + 六份 npm pack tarball + 本地 install + 13 项导入）作了独立复跑。
- 同意 A-018 / E-016：Hosted PR CI、修复版发布、tag、Release 均未发生。
- 相对 A-016 F-001：合并前 `pass` 的结构不变；顺序门禁转到修复版四包，且必须避开已发布的损坏 patch 组合。
- A-018 写「tarball 解包」；本条以现行 `npm-package-consumer-smoke.mjs` 为准，确认隔离方式已加固为 temp 目录下的真实 `npm install`。

## 结论 + 建议给编排器/用户的下一步

本 scope 判定 **pass**。开 PR 前没有未关闭的 required finding。本地修复候选对已确认的 ESM 与 schema 资产故障提供可复核修正。不要把本条 `pass` 写成 Hosted CI 或 npm 修复版已经完成。

建议 `/govern`：汇总 A-018 + A-019；从 `codex/npm-esm-consumer-fix` 开 PR；等 web job（含六包构建与隔离 smoke）全绿后合并；main CI 全绿后发布 protocol `0.2.17` / lib `0.1.16` / renderer `0.3.15` / ui `0.1.13`，做 registry 隔离消费校验，再 annotated `apps/api/v0.7.0` 与 GitHub Release。响应 F-001 / F-002。

## 声明

本意见不修改 status/progress；响应由 /govern 处理。
