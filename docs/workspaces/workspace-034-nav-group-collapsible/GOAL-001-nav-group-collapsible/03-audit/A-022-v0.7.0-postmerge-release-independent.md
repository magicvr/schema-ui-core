---
id: A-022-v0.7.0-postmerge-release-independent
goal_id: GOAL-001-nav-group-collapsible
doc: audit-entry
source: independent
auditor: Grok Build grok-4.6 (high)
type: execution-facts
scope: v0.7.0 post-merge pre-tag/release gate; E-017/A-021, PR #17 and main CI, registry npm versions and clean consumer, six platform CLI assets/SHA256SUMS, merge SHA 455df884, tag/Release absence
date: 2026-09-21
verdict: pass
created: 2026-09-21
updated: 2026-09-21
parent: null
version: 0.1.0
---

# A-022 · v0.7.0 合并后 tag 前发布门禁 independent 审计（2026-09-21）

- **source**：independent
- **auditor**：Grok Build grok-4.6 (high)
- **类型** / **scope**：execution-facts · PR #17 合并后、annotated `apps/api/v0.7.0` tag 与 GitHub Release 之前的发布门禁。核对工作区绑定、E-017、A-021、审计索引、PR/main CI、registry 六包与干净消费者、本地六平台 ZIP + SHA256SUMS，以及每个 CLI 的 `vcs.revision` 是否指向 merge commit `455df884754f7a16edd377644b60d75235e8e9bc`。
- **verdict**：**pass**（本门禁无未关闭 high required；用户已授权的 tag / Release 可以按既定资产集合推进。本条不把 tag 或 GitHub Release 写成已完成。）

## 范围与区间

工作区：`workspace-034-nav-group-collapsible`（`root_goal: GOAL-001-nav-group-collapsible`，`canonical_scope: docs/workspaces/workspace-034-nav-group-collapsible/`，`shared_materials_catalog: none`，`plan_refs` + `primary_plan: VP-034-nav-group-collapsible`）。Charter `schema-ui-core-admin-foundation@0.4.0` `active`。VP-034 `closed` v0.4.0。本意见只审本区 Root 的发布门禁，未读取其他工作区目标正文。

审计对象：

- 当前 `main` / `origin/main` = merge commit `455df884754f7a16edd377644b60d75235e8e9bc`（PR [#17](https://github.com/magicvr/schema-ui-core/pull/17)）
- [E-017](../02-execution/E-017-v0.7.0-postmerge-release-gates.md) 与 [A-021](A-021-v0.7.0-postmerge-release-self.md)
- Hosted PR CI run [35579164330](https://github.com/magicvr/schema-ui-core/actions/runs/35579164330) 与 main push CI run [35579828396](https://github.com/magicvr/schema-ui-core/actions/runs/35579828396)
- registry 上 protocol `0.2.17` / lib `0.1.16` / renderer `0.3.15` / ui `0.1.13` / shell `0.1.6` / theme `0.1.4`
- 本会话从 `registry.npmjs.org` 安装的隔离消费者（13 项导入）
- `C:\Users\magicvr\AppData\Local\Temp\schema-ui-v0.7.0-assets` 的六 ZIP、`SHA256SUMS`、ZIP 内二进制的 Go buildinfo
- 本地与远端是否已存在 `apps/api/v0.7.0` tag / GitHub Release

本意见未修改 `status` / `progress` / goal-tree / 决策或执行正文 / 实现代码，也未创建或推送 tag、未上传或创建 Release 资产。

## 成果（有证据）

| 检查项 | 结果 | 证据 |
|---|---|---|
| 工作区绑定 | 合格 | `workspace.md` Root / canonical / `plan_refs`+`primary_plan` 一致；资料目录 `none` |
| P-005 信息项 | 不影响本门禁 | I-034-001～005 均 `verified`，最晚阶段为 R1–R5；本发布门禁未引入新的到期 required 信息项 |
| 当前 HEAD | 与 merge SHA 一致 | 本地 `main` 与 `origin/main` 均为 `455df884…`；`git log -1` 为 `Merge pull request #17 from magicvr/codex/npm-esm-consumer-fix`；parents = `d083e337…`（main 前）+ `4af53755…`（PR head） |
| PR #17 | 已合并 | `state: MERGED`，`mergedAt: 2026-09-21T08:48:25Z`，`headRefOid: 4af53755facb109139323e96d4cba8508cf739c2`，`mergeCommit.oid: 455df884…` |
| PR CI | 9/9 success | run `35579164330`，event `pull_request`，`headSha=4af53755…`。web job 含 `Test npm package metadata and ESM compatibility` 与 `Build and import npm packages from a clean consumer`，均为 `success` |
| main CI | 9/9 success | run `35579828396`，event `push`，`headSha=455df884…`，workflow `r6-basic-matrix`，`conclusion: success`。九 job：web / api / api-postgres / 四组 browser E2E / 两组 container smoke。web job 同样执行上述 metadata 与隔离 tarball consumer 步骤，均为 `success` |
| CLI / 包面钉 | 与修复版一致 | merge SHA 上 `apps/api/cmd/schema-ui/main.go`：`apiVersion=v0.7.0`，protocol `0.2.17` / lib `0.1.16` / renderer `0.3.15` / ui `0.1.13` / shell `0.1.6` / theme `0.1.4`。`QUICKSTART.md` 与 `scripts/rewrite-lib-aliases.mjs` `versions` 同源 |
| registry 指定版本 | 可见且为 latest | 本会话 `npm view <name>@<ver> version` 返回精确版本；四修复包 `time` 为 2026-09-21T08:58–08:59Z（合并 08:48Z 之后）；四包 latest = 修复版。shell `0.1.6` / theme `0.1.4` 仍在 |
| 干净 registry 消费者 | 本会话复跑通过 | 临时目录 `schema-ui-v070-registry-consumer-*` 对 registry 安装六个钉住版本 + `react@19` + `react-dom@19`（**added 20 packages**）；已装 `package.json` 版本与上表一致；13 项动态导入全部 `PASS`，exit 0。已装 `@magicvr/schema-ui-*` 的 js/json **无**残留 `@schema-ui/` / `@schemas/`；protocol 含 `schemas/{action,node,page,reaction}.schema.json` 与 `protocol/host-support.json` |
| 六 ZIP + SHA256SUMS | 本地复核通过 | 六个 ZIP 的 SHA-256 与 `SHA256SUMS` 全部 MATCH。每个 ZIP 仅 1 个成员，成员名与对应平台二进制一致，字节数与同目录未压缩副本相同；ZIP 解压哈希 = 未压缩副本哈希 |
| CLI buildinfo | 指向 merge SHA | 六个 ZIP 解压二进制与未压缩副本：`go1.26.0`，`CGO_ENABLED=0`，`GOOS/GOARCH` 与文件名一致，`vcs.revision=455df884754f7a16edd377644b60d75235e8e9bc`，`vcs.modified=false`，path = `…/apps/api/cmd/schema-ui` |
| tag | **尚未存在** | `git tag -l apps/api/v0.7.0` 空；`git ls-remote --tags origin refs/tags/apps/api/v0.7.0` 空；远端无 `v0.7` 标签 |
| GitHub Release | **尚未存在** | `gh release view apps/api/v0.7.0` = release not found；releases API 仅有正式 [apps/api/v0.6.0](https://github.com/magicvr/schema-ui-core/releases/tag/apps/api/v0.6.0)（非 draft） |
| E-017 / A-021 事实面 | 与独立核对一致 | 合并 SHA、CI run id、npm 版本、13/13 消费者、六 ZIP 单文件、SHA 匹配、`vcs.revision`、tag/Release 未发生，均可重复核对。工作树相对 `455df884` 另有未提交的 E-017/A-021/索引，不进入本条 tag 对象 |

## 对照成功标准

| 标准 | 状态 | 证据 |
|------|------|------|
| A-019 F-001：Hosted PR CI 含新 web smoke 且全绿后才合并 | 已满足 | PR #17 MERGED；run 35579164330 9/9 success，web 含隔离 consumer 步骤 |
| A-019 F-002 / A-016 F-001：修复版 npm 先于 tag 可见并经 registry 消费 | 已满足（tag 仍未发生） | 四修复版 latest 可见；本会话 registry 隔离安装 13/13 PASS |
| main CI 在 merge SHA 上全绿 | 已满足 | 35579828396 · `headSha=455df884` · 9/9 success |
| 六平台 CLI 资产可追溯到 merge SHA | 已满足（本地暂存） | 六 ZIP SHA 匹配；buildinfo `vcs.revision=455df884…`、`vcs.modified=false` |
| annotated tag `apps/api/v0.7.0` | 未发生 | 本地与 origin 均无该 tag |
| GitHub Release 与资产上传 | 未发生 | 最新正式 Release 仍为 v0.6.0；本地 ZIP/sums 未上传 |
| 不把本地资产写成已发布 | 遵守 | E-017 / A-021 / 本条均记录资产位于本地临时目录 |

**本门禁判定：可以进入用户已授权的 annotated tag 与 GitHub Release。** `pass` 只覆盖 tag 前证据，不覆盖随后的 tag 对象、上传或 Release 页面。

## Findings

### F-001 · Release 上传集合应仅含六 ZIP + SHA256SUMS

- 严重度：low
- 建议：recommended
- 状态：open
- 描述：暂存目录除六个 ZIP 与 `SHA256SUMS` 外，还放有同名未压缩二进制。v0.6.0 已发布集合是 7 个 uploaded 资产（六 ZIP + sums）。`SHA256SUMS` 只覆盖 ZIP。创建 GitHub Release 时应只上传这七个文件，避免把未列入 checksums 的裸二进制一并挂上。
- 证据：`C:\Users\magicvr\AppData\Local\Temp\schema-ui-v0.7.0-assets` 同时存在未压缩二进制与 ZIP；E-012 记录 v0.6.0 Release 资产为七个 ZIP/sums。

无 required finding。下列为范围事实，不升为 finding：

- registry 上仍保留损坏组合 protocol `0.2.16` / lib `0.1.15` / renderer `0.3.14` / ui `0.1.12`。CLI 与 QUICKSTART 已钉到修复版；latest 已是修复版。
- 已发布 npm 包未暴露 `gitHead`。四修复版 `time` 晚于 PR #17 合并时刻，与 E-017 叙述一致。
- 本会话未重跑 Hosted CI，以 GitHub Actions API / `gh run view` 的 job 结论为准。
- 本会话未创建 tag、未 `gh release` 上传。

## 必改项汇总

无。tag 前开放 required = 0。

## 与既有意见的异同

- 同意 A-021 self `pass`：PR/main CI、修复版 npm、registry 消费者、六平台 ZIP/SHA、merge SHA 与 tag 未存在，均可独立复核。
- 同意 E-017 的证据边界：本地 registry 安装不替代 Hosted CI（本条同时核对了 PR 与 main 的 web smoke 步骤）；npm 发布已发生，tag/Release 尚未发生。
- A-019 F-001 / F-002（及 A-016 F-001 的修复版顺序）在本 scope 下已有独立关闭证据；本条不重新打开它们。
- 相对 A-021：补记暂存目录含未压缩二进制，作为 Release 上传集合的 recommended（F-001）；A-021 未把该目录形状写成风险。

## 结论 + 建议给编排器/用户的下一步

本 scope 判定 **pass**。用户已授权的 `apps/api/v0.7.0` annotated tag 与 GitHub Release 可以推进。tag 对象应为 merge commit `455df884754f7a16edd377644b60d75235e8e9bc`。Release 资产建议仅为：

- `schema-ui-v0.7.0-darwin-amd64.zip`
- `schema-ui-v0.7.0-darwin-arm64.zip`
- `schema-ui-v0.7.0-linux-amd64.zip`
- `schema-ui-v0.7.0-linux-arm64.zip`
- `schema-ui-v0.7.0-windows-amd64.zip`
- `schema-ui-v0.7.0-windows-arm64.zip`
- `SHA256SUMS`

上传后按 v0.6.0 E-012 惯例从 Release 再下载并按 `SHA256SUMS` 复核。不要把本条 `pass` 写成 tag 或 Release 已经完成。

建议 `/govern`：汇总 A-021 + A-022；按上述 SHA 创建并推送 annotated tag；创建 GitHub Release 并只上传七个 checksummed 资产；响应 F-001；补写 tag/Release 执行事实。

## 声明

本意见不修改 status/progress；响应由 /govern 处理。
