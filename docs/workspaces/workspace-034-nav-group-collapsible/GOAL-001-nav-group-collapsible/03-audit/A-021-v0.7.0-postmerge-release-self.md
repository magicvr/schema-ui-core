---
id: A-021-v0.7.0-postmerge-release-self
goal_id: GOAL-001-nav-group-collapsible
doc: audit-entry
source: self
auditor: current-session
type: execution-facts
scope: v0.7.0 pre-tag release gate after PR #17; Hosted PR/main CI, corrected npm registry packages and consumers, six platform CLI assets/checksums
date: 2026-09-21
verdict: pass
created: 2026-09-21
updated: 2026-09-21
parent: GOAL-001-nav-group-collapsible
version: 0.1.0
---

# A-021 · v0.7.0 pre-tag release self audit（2026-09-21）

- **source**：self
- **类型 / scope**：execution-facts · PR #17 合并后核对 main CI、npm 修复 patch 的 registry 可见性与消费者导入、六平台 CLI ZIP / SHA256SUMS。
- **verdict**：**pass**（npm 与 CI 门禁满足，可进入 tag 前 independent audit；不代表 tag 或 GitHub Release 已完成。）

## 范围与区间

工作区 `workspace-034-nav-group-collapsible`，Root `GOAL-001-nav-group-collapsible`。`workspace.md` 中 Root / canonical scope / `plan_refs` / `primary_plan` 一致，`shared_materials_catalog: none`；I-034-001～005 均 verified。本次不修改 Root、workspace 或 VP 状态。

## 成果（有证据）

| 检查项 | 结果 | 证据 |
|---|---|---|
| PR #17 合并前 CI | 通过 | run `35579164330`，9/9 jobs success；head `4af53755…` |
| main CI | 通过 | run `35579828396`，9/9 jobs success；head = merge commit `455df884754f7a16edd377644b60d75235e8e9bc` |
| 新增包 smoke 门禁 | 通过 | PR 与 main web job 均执行 build、rewrite/finalize、六包 npm tarball pack/install 与隔离消费者导入 |
| npm 修复版本 | 已发布且可见 | protocol `0.2.17`、lib `0.1.16`、renderer `0.3.15`、ui `0.1.13`；四项 `npm view` 返回指定版本 |
| registry 消费者 | 通过 | 临时消费者从 registry 安装六个包及 React peers / dependencies（20 packages）；6 个根入口 + 7 个代表子路径共 13 项导入 `PASS` |
| 六平台 CLI 资产 | 本地准备完成 | 六 ZIP 只含对应单个二进制；每个 build info 均为 Go 1.26、`CGO_ENABLED=0`、`vcs.revision=455df884…` |
| SHA256SUMS | 本地复核通过 | 六个 ZIP 的 SHA-256 与 `SHA256SUMS` 全部匹配 |
| tag / Release | 未发生 | `apps/api/v0.7.0` annotated tag 尚无；ZIP / sums 未上传；GitHub Release 未建立 |

## Findings 与既有意见响应

- A-019 F-001（PR Hosted CI 在 merge 前全绿）：已满足，run `35579164330` 为 9/9 success。
- A-019 F-002（main CI 后先发布 npm 修复包并验证 registry consumer，再 tag）：npm 发布与 registry 消费验证均已满足；下一步仍需完成 tag 前 independent audit，再按此顺序创建 tag / Release。
- A-018 F-S-002（finalization / ESM consumer 回归）：A-019 独立复核修复候选，A-021 再以 Hosted PR/main smoke、registry 安装结果与六资产本地验证确认，没有重新打开该 finding。

本 scope 当前无开放 required finding。tag / 上传资产 / 创建 Release 是尚待发生的外部门禁事实，不记成已完成，也不在本条中提前执行。

## 结论

PR 与 main CI、npm 修复包发布及 registry 消费门禁全部满足。六平台资产本地构建与哈希验证通过。根据 release 风险仍须完成 tag 前 independent audit；在其确认前不创建 `apps/api/v0.7.0` tag。
