# 目标树 · workspace-025-config-export-diff-dryrun-import

## 树

```text
GOAL-001-config-export-diff-dryrun-import（active · 0/4 · primary_plan = VP-025-config-export-diff-dryrun-import）
└── 纲领阶段（子目标待立项）：
    R1 合同冻结 → R2 导出+diff → R3 dry-run+导入 → R4 证据与关门
```

## 状态表

| id | title | status | progress | parent |
|----|-------|--------|----------|--------|
| GOAL-001-config-export-diff-dryrun-import | 配置包导出 / diff / dry-run / 导入 | active | 0/4 | null |

> 子目标按纲领阶段逐项立项后同步本树。progress 仅由 R1～R4 显式检查点等权计算；不放行阶段、不关闭 finding。