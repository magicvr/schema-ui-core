---
id: E-003-s3-ux-p0
doc: execution-entry
goal: GOAL-012-w11-mfa-ux-review
date: 2026-08-15
status: done
parent: GOAL-001-design-implementation-conformance
created: 2026-08-15
updated: 2026-08-15
version: 0.1.0
---

# E-003 · S3 UX P0（U-01 角色多选 + U-02 权限动态化）

## 事实（全部已落地并验证）

1. **optionsSource 渲染能力**（本地扩展）：form-controls.ts FormControlField 新增 optionsSource/optionsMapping；form-controls.tsx useDynamicOptions 钩子（{items:[...]}/裸数组；加载中回退静态 options；非法源/失败 fail-closed；query 串允许、scheme/host/# 拒绝）；select/radio/checkboxGroup 三控件接入；render.ts 字段解析白名单放行；render.tsx 向 FormControls 注入 crud fetcher（auth-aware）；formComponent props 类型同步。
2. **后端目录端点**：authsession.Repository 新增 ListPermissionCatalog / ListMenuItemCatalog（permissions / menu_items 表，与授权校验同源）；handler/rbac_catalog.go 新增 GET /api/permissions、GET /api/menu-items（auth + roles.read）；roles provider 注册并声明路由；rbac_catalog_test.go 覆盖（匿名 401 / viewer 可读 / 数据形状）。
3. **schema 改造**：users.json openRoles → checkboxGroup 动态选项（/api/roles?pageSize=1000，value=key/label=name）；roles.json openCreate/openEdit 的 permissions → /api/permissions（value=key）、menuItems → /api/menu-items（value=id/label=label）。
4. **i18n**：schema.users.field.roles 文案改为「角色 / Roles」（去除逗号分隔提示）。
5. **验证**：form-controls.dynamic-options.test.tsx 4 用例（加载、静态回退、失败空集、非法源不请求）；go build/tests 绿；tsc 0；全量回归见 E-004。
