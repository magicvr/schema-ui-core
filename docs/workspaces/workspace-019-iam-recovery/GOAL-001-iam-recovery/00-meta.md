---
id: GOAL-001-iam-recovery
title: IAM：密码策略 / 邀请入职 / 自助恢复状态机
status: active
parent: null
created: 2026-08-25
updated: 2026-08-25
version: 0.4.0
progress: 3/4
plan_refs:
  - VP-019-iam-recovery
primary_plan: VP-019-iam-recovery
serves_summary: 承载 VP-019（Admin 功能 · IAM）实现：自助恢复状态机（消费 VP-018 已校验邮箱 + VP-017 MailSender）、密码策略产品面、邀请入职。不承载 SMS、模板中心、组织权限、OIDC 或业务域。
---

# GOAL-001 · IAM：密码策略 / 邀请入职 / 自助恢复状态机

## 概述

本 Root 承载 [VP-019-iam-recovery](../../../vision/plans/VP-019-iam-recovery.md)（**`active`** · 2026-08-25 激活，VRev-043 independent `pass`）的实现：在 `core.auth-session`、VP-018 账号邮箱身份（`users.email` + 校验状态机）与 VP-017 内核 `MailSender`（可切换渠道）之上，把忘记密码收成可核对的自助恢复闭环，并把密码策略与邀请入职收成同一 IAM 波次的 Admin 能力。

**激活门禁已全部满足**（VR-047）：VRev-043（grok build · grok-4.6 · high）`pass`（0 required）；Admin 类 freshness PASS（`092bf37` → `66f5fd1f`，不暂挂 `go`）；VP-009/VP-010 无开放阻断。

**边界**：不承接 SMS / 推送 / IM、模板中心、多邮箱 / 别名 / 邮箱即登录名、组织 / 部门 / 岗位与数据权限 `org`、OIDC/SSO/SCIM、业务域；管理员重置保持 `must_change_password` 特权路径；不改 Profile 默认集 / 模块矩阵 / Manifest 装配语义。安全/符合性 gap → VP-009 / VP-010。

## 纲领路线图（P-001 · V-F076 fixed）

| 阶段 | 内容 | 先后 | 状态 |
|------|------|------|------|
| R1 | **合同冻结**：证明形态（默认候选 = VP-018 6 位码，I-001）、TTL/冷却最晚 R2 方案冻结前（I-002）、策略默认参数与配置边界（I-003，UI 仅 `admin.settings`、强制面 `core.auth-session`、禁止加入 mvp 默认集）、邀请形态（I-004/005）、无邮箱边界（I-006，2026-08-22 产品事实 registered）、策略对既有账号生效边界（I-007）、会话语义（I-008）、MFA 与自助恢复（I-009，防 MFA 旁路） | 起点 | **已完成**（GOAL-002 done · A-001 self pass；D-002 + GOAL-002 D-001 五节条款） |
| R2 | **自助恢复全链**：登录页发起 → 经已校验邮箱投递 → 校验 → 设新密码 → 恢复登录；过期/重发冷却；MFA 语义按 I-009 | 依赖 R1 | **已完成**（GOAL-003 done · A-001 independent conditional→归零 + A-002 self pass；迁移 0056 + start/complete 公开面 + MFA 第二因子门 + Web 两步流） |
| R3 | **密码策略 + 邀请入职**：策略配置面与强制面、渐进生效；管理员生成邀请（邮件/链接）、受邀激活、有效期/撤销 | 依赖 R2 | **已完成**（GOAL-004 done · A-001 independent conditional→归零 + A-002 self pass；迁移 0057–0059 + 四口强制 + 邀请全链 + Web 面） |
| R4 | **证据与关门**：端到端经 VP-017 现行渠道取信；策略强制可核对；邀请全链；无越界（SMS/模板/多邮箱/业务域）；required = 0 | 依赖 R3 | 待启动 |

`progress` = 已完成阶段数 / 4。当前 **3/4**（R1/R2/R3 关门 · 2026-08-25）。

## 成功标准（方向级）

1. 自助恢复全链落地且可核对：登录页发起，经已绑定+已校验邮箱投递（现行渠道），重发冷却与过期语义可验证；完成后设新密码并正常登录。
2. 密码策略已配置并强制：创建 / 管理员重置 / 自助恢复的新密码均过策略校验；策略变更不强制登出、不锁死既有账号。
3. 邀请入职落地：管理员可生成邀请（邮件或链接形态），受邀者可激活账号并设置密码；有效期与撤销语义可核对。
4. 全部投递经 `kernel.MailSender`（当前渠道），无第二运输；无生产渠道时 mock 出站记录可端到端取信。
5. 管理员重置保持 `must_change_password` 特权路径；未引入 SMS / 模板中心 / 多邮箱 / 业务域；未改 Charter；未改 Profile 默认集作为本波成功条件。
6. 开放 required finding = 0（或已合法闭合）。

## 信息就绪与未知项

与 VP-019 I-019-00X 同号镜像（D-001）。禁止在 R1 关闭前直接改恢复/邀请/策略 DDL 或迁移台账。

| ID | 级别 | 所需信息 / 问题 | 影响门禁 | 最晚需要阶段 | 验证 / 收集动作 | 状态 | 延期 / 复核 | 证据 / 结论 |
|----|------|-----------------|----------|--------------|-----------------|------|-------------|-------------|
| I-001 | required | 自助恢复证明形态：验证码 vs 魔术链接（默认候选 = VP-018 6 位码） | 方案冻结 | R1 | 用户裁决 | **verified**（D-002） | — | 6 位邮箱验证码（2026-08-25 裁决） |
| I-002 | required | 恢复令牌/验证码 TTL 与重发冷却 | 方案冻结 | **R2 方案冻结前** | 用户裁决 | **verified**（D-002） | — | TTL 10 分钟 / 冷却 60 秒 / 错 5 次作废（对齐 VP-018；2026-08-25 裁决） |
| I-003 | required | 密码策略默认参数与配置边界（UI 仅 `admin.settings`；强制面 `core.auth-session`；禁止加入 mvp 默认集） | R2 方案冻结 | R2 | 用户裁决 | **verified**（GOAL-002 D-001 §2） | — | 起步宽松：默认 min 8 字节、复杂度/历史关；可配置收紧（2026-08-25 裁决） |
| I-004 | required | 邀请形态：邮件投递 vs 管理员出示链接；目标是否须预置账号 or 邀请即建号 | R3 方案冻结 | R3 | 用户裁决 | **verified**（GOAL-002 D-001 §3） | — | 双形态并存 + 邀请即建号（2026-08-25 裁决） |
| I-005 | required | 邀请有效期 / 失效 / 撤销 / 一次性语义 | R3 方案 / 实施 | R3 接入前 | 用户裁决 | **verified**（GOAL-002 D-001 §3） | — | 默认 7 天 · 一次性 · 可撤销 · 重发撤旧发新（2026-08-25 裁决） |
| I-006 | required | 无邮箱账号的自助恢复边界：仅管理员重置 | 方案冻结 | R1 | 产品事实投影 | **registered**（2026-08-22 用户确认；R1 验收到位） | — | 无邮箱不自助、走管理员重置 |
| I-007 | required | 密码策略对既有账号生效边界（渐进 vs 登录强制改密） | R2 方案冻结 | R2 | 用户裁决 | **verified**（GOAL-002 D-001 §2） | — | 渐进：仅设密时刻强制，不扫描存量、不强登出（2026-08-25 裁决） |
| I-008 | non-blocking | 恢复 / 邀请 / 改密后的会话语义（是否全端登出或撤销既有 session） | 方案冻结 | R4 | 用户裁决 | **verified**（GOAL-002 D-001 §4） | — | 投影现行设密语义（token_version 前移撤销其余会话）（2026-08-25 裁决） |
| I-009 | required | 已登记 MFA 的账号如何走自助恢复（绕过第二因子 / 要求 TOTP / 拒绝自助只走管理员重置）——防 MFA 旁路（V-F076） | 方案冻结 | R1 合同冻结 | 用户裁决 | **verified**（D-002） | — | 完成设新密码前要求第二因子（TOTP/恢复码）；缺失走管理员重置（2026-08-25 裁决） |

## 父目标

- null（Root；Charter `schema-ui-core-admin-foundation@0.2.0` / VP-019-iam-recovery）

## 台账布局

新目标为三个可追加台账创建同名平铺目录：`01-decision/`、`02-execution/`、`03-audit/`。索引文件保留 frontmatter、摘要和条目索引；独立记录使用 `D-NNN-*`、`E-NNN-*`、`A-NNN-*` 文件。