---
id: E-002-r3-c1-i006-scan
doc: execution
goal: GOAL-004-r3-bounded-pilot
date: 2026-08-05
status: recorded
---

# E-002 · C1 I-006 入口盘点

## 事实

完成了 Web 静态 Manifest、API 嵌入式 Manifest、API Schema embed、Vite/Nginx
代理、Docker 最终层、中心 Handler 注册、Kernel Profile 和 Web Host/Shell
事件的源码盘点。关键出处和当前缺口记录在
`attachments/r3-c1-i006-boundary.md`。

## 当前结论

- API `manifest.ForModules(plan.IDs())` 已在组合根接入；生产运行时的 Manifest
  不是 Web `dist` 中的静态副本。
- Web `public` Manifest 仍被多个测试读取，Docker 构建后删除最终 `dist` 副本；
  因此源码 fixture 与生产静态兜底必须分开治理。
- `handler.Register` 当前固定挂载 Settings/Activity/Operations；MVP 仍会暴露
  Settings/Activity 后端路由，说明路由启停尚未达到 Profile 语义。
- `BRANDING_CHANGED_EVENT` 的触发点在 Host，消费点在 App/LoginPage，通用
  Renderer 没有该分支；但目前没有独立事件测试，C2/C3 必须补证。

## 限定

本记录是 C1 事实收集，不是 C2 实施、C3 验收或生产/CI 证据。
