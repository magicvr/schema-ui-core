# 架构决策讨论稿：采用模块化插件架构替代双线代码树

> **状态**：提议决策（Proposed for Decision）  
> **目标**：统一技术架构，降低长期维护成本，确立可执行的模块化落地路径  
> **关联**：本稿通过后，将形成正式 ADR，并指导下一迭代的试点工作

---

## 1. 动机：我们为什么要改？

当前状态：
- 我们完成了 MVP 构建，接下来要往更完整、更可扩展的方向演进。
- 初期规划的“MVP 基架线”与“完整 Admin 线”**两套代码树并存**，初衷是给 fork 用户不同起点。
- 实际开发中发现：
  - **维护成本翻倍**：修复一个通用 bug 要改两处；新增一个通用能力要在两条线各做一遍。
  - **不必要**：因为 **Schema-UI 协议**的存在，前后端已经具备了“后端扩展、前端不动”的基础。Admin 类功能的增删，本质上只是后端模块装配问题，不应导致前端代码分支。

**核心判断**：继续维护两条代码树是亏本的。我们应该用“**单主线 + 模块注册 + 启动配置**”替代“双线代码树”。

---

## 2. 目标架构愿景（我们要去的地方）

### 2.1 总体形态

```
┌──────────────────────────────────────────────────────────────────────────┐
│                           单主线代码仓库                                 │
├──────────────────────────────────────────────────────────────────────────┤
│  内核 (Kernel)     │  功能模块 A  │  功能模块 B  │  功能模块 C          │
│  - HTTP 引擎       │  (users)     │  (roles)     │  (orders)            │
│  - 身份认证管道     │  + routes    │  + routes    │  + routes            │
│  - 轻量 DI 容器     │  + schema    │  + schema    │  + schema            │
│  - 模块注册调度器    │  + perms     │  + perms     │  + perms             │
│  - 迁移运行器       │  + nav       │  + nav       │  + nav               │
│  - Manifest 聚合器  │  + mig       │  + mig       │  + mig               │
│  - 配置加载器       │  + manifest  │  + manifest  │  + manifest          │
│                    │  + depends   │  + depends   │  + depends           │
│                    │  + config    │  + config    │  + config            │
│                    │  + seeds     │  + seeds     │  + seeds             │
└──────────────────────────────────────────────────────────────────────────┘
                              │
                              ▼
              ┌─────────────────────────────────┐
              │  启动时 Profile 配置             │
              │  (config.modules.enabled)       │
              │  mvp / admin / custom           │
              └─────────────────────────────────┘
```

### 2.2 关键原则（必须达成共识）

| # | 原则 | 含义 |
|---|------|------|
| P-1 | **单主线** | 不再维护两套长期代码分支；所有开发在主干的同一套代码上进行。 |
| P-2 | **薄内核 + 模块** | 内核只包含启动、鉴权、路由挂载、迁移运行、Manifest 聚合、配置加载等基础能力；所有业务能力都是模块。 |
| P-3 | **轻量 IoC/DI 管理** | 模块通过 DI 容器注册依赖和贡献点；内核不硬编码任何业务模块的导入。 |
| P-4 | **前端跟随协议，不跟随模块** | 在 Schema-UI 协议覆盖范围内，后端增减模块**不要求前端做任何改动**；前端只随协议版本演进。 |
| P-5 | **Profile = 装配列表** | “MVP/Admin/Custom”在产品文档里是不同起点的推荐配置，在工程上就是 `enabled` 列表的差异。 |
| P-6 | **Manifest 由后端驱动** | 前端启动时从后端拉取完整的应用清单（Manifest），不再依赖前端静态手写文件。 |

---

## 3. 需要拍板的 8 个关键决策维度

以下每个维度，我们都给出**建议选项**，请评审会确认或修正。

### 维度 D-1：IoC / DI 的引入程度

| 选项 | 描述 | 评价 |
|------|------|------|
| A（推荐） | 引入轻量 DI 框架（如 Uber Fx / Google Wire），模块只负责声明 `Provide`，内核负责组装和生命周期 | 显式、可测试、启动路径清晰；模块间不直接 import 实现 |
| B | 仅保留手写 `Register()` 调用，不引入 DI 框架 | 代码量随模块数线性增长；模块间依赖需手工维护顺序 |

> **建议拍板**：**选 A**。这是模块化落地的基础设施，能从根本上降低模块间的耦合成本。

---

### 维度 D-2：Schema-UI 协议的覆盖边界（决定前端动不动的红线）

| 范围 | 是否由协议覆盖 | 前端是否允许因业务模块而改动 |
|------|---------------|------------------------------|
| 标准管理界面（列表/详情/表单/操作按钮/导航菜单） | **是**，100% 覆盖 | **禁止**，一律由后端 schema 驱动 |
| 复杂仪表盘 / 自定义图表 / 特殊交互组件 | 纳入下一阶段协议扩展 | 仅当协议版本升级时统一改，不为单个模块特制 |

> **建议拍板**：**以标准 CRUD 管理界面为红线**，红线内前端零改动。红线外作为协议演进，不破坏模块化原则。

---

### 维度 D-3：模块的自治颗粒度与注册契约（含依赖、配置与种子数据）

每个模块必须是一个自包含的 Go package，并通过 DI 容器向 Kernel 注册以下 **9 项标准贡献点**：

| 贡献点 | 说明 | 是否必须 |
|--------|------|----------|
| 路由（Routes） | HTTP 路径与 Handler | ✅ 必须 |
| Schema 页面定义 | 符合 Schema-UI 协议的 JSON/YAML 描述（可内嵌或外部加载） | ✅ 必须 |
| 权限声明（Permissions） | 该模块引入的权限 key 列表 | ✅ 必须 |
| 菜单/导航贡献（NavItems） | 菜单项、路由别名、图标引用 | ✅ 必须 |
| **Manifest 片段（Pages + Features）** | **该模块在全局应用清单中注入的页面路由和功能开关** | ✅ **必须（支撑 D-6）** |
| 数据库迁移（Migrations） | 模块自带的**表结构**迁移文件（按命名空间隔离，如 `users/001_create_table.sql`） | ✅ 必须 |
| **依赖声明（DependsOn）** | **声明本模块运行所依赖的其他模块 ID 列表（如 `activity` 依赖 `users`）** | ⚠️ **可选但强烈建议**（无依赖可留空） |
| **配置定义（Config Definitions）** | **模块声明的配置项（类型、默认值、校验规则、描述），Kernel 统一加载并合并到全局配置** | ⚠️ **可选**（无配置可留空） |
| **种子数据（Seed Data）** | **模块的初始数据（如默认权限分配、初始菜单、系统默认值），仅在全新安装（fresh install）时执行** | ⚠️ **可选**（无种子可留空） |

> **关于贡献点的工程约束**：
>
> **`DependsOn`（依赖声明）** ：
> - Kernel 在启动时做**拓扑排序**，按依赖顺序注册模块。
> - 若 `DependsOn` 中声明的模块不在当前 `enabled` 列表中，**Kernel 启动失败并报错**（Fail-fast），避免运行时出现残缺界面或空指针。
> - 禁止循环依赖（检测工具在 CI 中拦截）。
>
> **`Config Definitions`（配置定义）** ：
> - 每个模块声明自己的配置结构（支持嵌套），Kernel 负责从配置文件/环境变量加载并校验。
> - 配置定义可通过 API 暴露给前端（如 Settings 页面），由 Schema-UI 自动渲染配置表单，**避免前端为每个模块的设置页单独写代码**。
> - 示例：`activity` 模块声明 `retention_days: 30`，Kernel 加载后注入模块。
>
> **`Seed Data`（种子数据）** ：
> - 与 `Migrations` 严格区分：**迁移管表结构，种子管初始数据**。
> - 种子数据仅在 **新数据库（无 `schema_migrations` 历史）** 时执行；已存在的实例不自动覆写，避免覆盖用户数据。
> - 如需升级种子数据（如新增默认角色），应通过迁移或显式升级脚本处理，不在种子层做更新。

---

### 维度 D-4：迁移（Migration）策略（最关键的工程约束）

| 选项 | 描述 | 风险 |
|------|------|------|
| A（推荐） | **模块自带迁移**，按命名空间隔离（如 `users/001_init.sql`）；核心运行器负责按序执行。**禁用模块时，不自动回滚/删除数据表**，仅隐藏路由和菜单。 | 数据库会残留表；产品文档需明确说明“禁用不等于数据清除”。 |
| B | 支持运行时迁移回滚（禁用时自动删表） | 外键约束、历史数据丢失风险极高，几乎不可行。 |

> **建议拍板**：**选 A**。我们的场景是“新 fork 选配模块”为主，“已部署实例动态减模块”为辅。禁用模块 = 业务不可见，不强制物理删除数据。

---

### 维度 D-5：Profile 的工程实现形式

| 选项 | 描述 | 评价 |
|------|------|------|
| A（推荐） | **运行时 + 启动固定**：读取配置文件 `modules.enabled: [users, roles, ...]`，启动时决定注册哪些模块；启动后不可动态变更。 | 灵活、可观测、fork 用户直接改配置即可切换起点。 |
| B | 编译时（build tags） | 需要多次编译；不灵活。 |

> **建议拍板**：**选 A**。产品文档里的“MVP 线”对应 `enabled` 中最小集合；“Admin 线”对应全量集合；“Custom”由用户自己组合。

---

### 维度 D-6：Manifest（应用清单）的权威来源（阻塞 V-3 的关键）

| 选项 | 描述 | 对“前端零改动”的影响 |
|------|------|------------------------|
| A（**强烈推荐**） | **运行时后端聚合**：后端提供一个标准 API（如 `GET /.well-known/schema-ui/manifest`），由 Kernel 汇总所有已启用模块的 `pages`、`navigation` 和 `features` 片段后统一返回。前端启动时只调此 API。 | ✅ **完全支撑 V-3**。增减模块只需改后端 `enabled` 列表，前端 manifest 自动变化，代码零 Diff。 |
| B | 构建时合并：在 CI 或编译阶段，根据当前 `enabled` 列表将各模块的 manifest 片段合并写入前端 `public` 目录。 | ❌ 增减模块需重新构建前端容器或重发静态资源，无法做到“纯后端操作”。 |
| C | 保持现状（手写前端静态 `app-manifest.json`） | ❌ 完全破坏模块化初衷，每加一个业务模块都得改前端仓。 |

> **建议拍板**：**选 A**。这是实现“后端增减模块，前端不动”的**唯一技术前提**。同时，Kernel 需提供 Manifest 聚合器接口，供各模块通过注册契约注入片段。

---

### 维度 D-7：实施节奏（避免“既要又要”）

| 选项 | 描述 | 评价 |
|------|------|------|
| A（推荐） | **试点先行**：先选 1 个与核心耦合最弱的功能模块（如 Activity Log）按新架构重写/抽取，验证全链路，再逐步迁移 users/roles/settings 等。 | 风险可控，快速验证机制可行性。 |
| B | 全量重构 | 风险高，周期长，阻塞正常迭代。 |

> **建议拍板**：**选 A**。试点周期控制在 1~2 个迭代内，验证通过后立即启动存量模块迁移计划。

---

## 4. 双线代码树的处理方案（明确收尾）

| 今日双线概念 | 模块化后的替代方案 |
|-------------|-------------------|
| MVP 基架代码树 | 删除独立分支；用 **profile=mvp**（最小 `enabled` 列表）替代 |
| Admin 完整代码树 | 删除独立分支；用 **profile=admin**（全量 `enabled` 列表）替代 |
| 产品文档中的“双线”描述 | 改为“系统提供 MVP / Admin 两种推荐装配配置，用户可自定义裁剪” |
| 后续新增业务能力 | 一律以**新模块**方式开发，不再新增任何长期发布分支或代码树副本 |

> **关键承诺**：代码层面**不再保留两套业务代码副本**。产品文档层面保留“多起点”叙事，但起点只是一份配置文件。

---

## 4.5 与现有代码的真实距离（审计事实，不避重就轻）

当前代码库距离上述目标尚有 **4 个必须拆除的障碍**。若不在试点中正面解决，模块化将沦为“新模块自嗨，老模块依旧一团乱麻”。

| 现有病灶 | 模块化目标态 | 试点必须完成的重构动作 |
| :--- | :--- | :--- |
| **中心化巨型 Register**：`handler.Register` 里手写挂载了所有业务路由 | 业务路由由各模块自己通过 DI 注册，Kernel 只负责挂载 | 试点模块（如 Activity）必须**率先从 Register 中拆出**，改为自注册模式，证明 Kernel 接口可行 |
| **全局 Schema Fixtures**：所有页面 Schema 集中在 `fixtures/schema/`，与模块隔离 | Schema 定义归属到各自模块包内（`activity/schemas/`），Kernel 仅提供加载器 | 试点模块必须带走自己的 Schema 文件，并修改内核加载逻辑支持按模块目录扫描 |
| **静态前端 Manifest**：`public/app-manifest.json` 手写全量业务页表 | Manifest 由后端运行时聚合（D-6） | 试点必须实现 Manifest 聚合 API，并将自身片段注入其中，前端切换为调用此 API（可先做兼容：静态文件降级） |
| **Host 层特例副作用**：如 `settings` 的 `notifyBrandingChanged` 直接写在 host | Host 仅提供事件总线或中间件钩子，业务副作用由模块自己订阅 | 试点必须识别出 host 中是否有对业务模块的硬编码特判，将其抽象为通用事件或删除 |

> **共识声明**：通过试点，我们不是“新增一个 Activity 模块就完事”，而是**用 Activity 做手术刀，把上述 4 个病灶切除干净**，证明 Kernel 切口正确。否则后续迁移 users/roles 时，会在同一个泥潭里反复挣扎。

---

## 5. 试点计划：以 Activity 为手术刀，切除旧架构病灶

### 5.1 试点范围（比“新增模块”更重，但必须）

选定 **Activity Log（操作日志）** 作为试点。理由不变（耦合弱），但**试点工作的核心目标不再是“写出 Activity 模块”，而是“用 Activity 证明我们能干掉上述 4 个障碍”**。

**试点交付物（必须全部完成）** ：

1. Activity 自身按新模块标准（含 DependsOn、Manifest 片段、Config、Seeds）完整实现。
2. **拆除中心化 Register 中对 Activity 的挂载**，改为模块自注册。
3. **将 Activity 的 Schema 从全局 fixtures 移到模块内**，并调整 Kernel 加载逻辑。
4. **实现后端 Manifest 聚合 API**，Activity 的页面/菜单通过此 API 暴露；前端至少完成对接联调（可先双轨运行）。
5. 排查 Host 层是否硬编码了 Activity 相关逻辑，若有则抽象掉。

### 5.2 试点验证目标（V-1 ~ V-4）

| 编号 | 验证项 | 验收标准 |
|------|--------|----------|
| V-1 | 模块注册与启动 | `enabled` 列表中加入 activity，启动后路由可访问、菜单出现 |
| V-2 | 模块禁用 | 从 `enabled` 列表移除，重启后路由返回 404、菜单消失 |
| V-3 | 前端零改动 | 同一个前端 build 对接“含 activity”与“不含 activity”的后端，页面集随 manifest 变化，前端代码无任何 diff |
| V-4 | Schema 贡献 | Activity 的列表/详情页完全由后端 schema 驱动，前端 Renderer 通用渲染 |

### 5.3 试点后的决策门

- 若上述 5 项交付物 + V-1~V-4 全部通过 → 证明 Kernel 切口正确，立即启动存量模块迁移计划。
- 若卡在第 3 或第 4 项（Kernel 改造阻力大）→ 说明 Kernel 抽象不足，需先花 1 个迭代加固 Kernel 再继续，**不盲目推进全量迁移**。

---

## 6. 明确非目标（划清边界，防止范围蔓延）

- ❌ 不支持运行时热插拔（启动后动态增删模块）。
- ❌ 不支持 `.so` / 二进制插件机制。
- ❌ 不引入全量 Spring 式 AOP 容器（但允许中间件链式扩展）。
- ❌ 不承诺“任何界面交互都由 schema-ui 覆盖”——复杂可视化仍可能走协议演进。
- ❌ 本决策不涉及前端 Renderer 本身的架构重构，仅约束其与模块的交互方式。
- ❌ **不把“试点”定义为“只写新模块，不碰旧代码”**——试点必须动手术切除旧架构病灶，否则试点无意义。

---

## 7. 需要你们在会上确认的唯一清单（请逐条表态）

| 编号 | 决策项 | 建议选项 | 您的意见（✅/❌/修正） |
|------|--------|----------|------------------------|
| 1 | 是否采用轻量 DI 框架（Fx/Wire）？ | D-1 → A | |
| 2 | 是否以“标准管理界面”为红线，红线内前端零改动？ | D-2 → 红线内禁止改 | |
| 3 | 模块契约是否为 **9 项贡献点**（路由/schema/权限/导航/Manifest片段/迁移/依赖声明/配置定义/种子数据）？ | D-3 → 是 | |
| 4 | 迁移策略是否采用“禁用不删表，仅隐藏路由/菜单”？ | D-4 → A | |
| 5 | Profile 是否采用“运行时配置 + 启动固定”？ | D-5 → A | |
| 6 | Manifest 是否采用 **运行时后端聚合 API**？ | D-6 → A | |
| 7 | 是否以 Activity Log 为试点，先验证再全量迁移？ | D-7 → A | |
| 8 | 是否同意“删除 MVP / Admin 两条长期代码分支，统一到单主线 + 配置”？ | §4 核心承诺 | |
| 9 | 试点是否承诺“切除 §4.5 中的 4 个旧架构病灶”，而非仅仅新增模块？ | §4.5 / §5.1 | |

---

## 8. 后续步骤（若以上全部通过）

1. **本稿归档为正式 ADR**（架构决策记录），作为后续开发的权威依据。
2. **排期试点**：安排 1~2 个迭代完成 Activity Log 模块化试点（含 §4.5 的 4 项拆除工作）。
3. **起草《模块开发指南》**：为后续新增模块和存量迁移提供具体操作规范（目录结构、注册方式、迁移命名、DependsOn/Config/Seeds 声明规范等）。
4. **启动存量迁移计划**：按 users → roles → settings → 其他模块的顺序，逐步迁移至新架构。

